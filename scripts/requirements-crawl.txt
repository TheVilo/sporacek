# Závislosti pre scripts/crawl_ceny.py (lokálny zber cien).
# Inštalácia:
#   pip install -r scripts/requirements-crawl.txt
#   playwright install chromium
#
# LLM režim (web-letáky) používa Gemini cez litellm, ktorý je závislosťou crawl4ai.
crawl4ai>=0.4.0
